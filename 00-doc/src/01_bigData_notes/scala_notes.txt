

--------------------------------------------------------------java-------------------------------------------------------------
String是不可变字符转，stringbuild和stringbuffer是可变字符串
String指向地址，指向不变，地址数据可以变但不变，通常引用数据生成新的字符串和地址
隐式转化：byte（8）=》short（16）=》int（32）=》long（64）=》float（32，存的科学计数法）=》double（64）   char（16） =》int（32）  byte（8）无法转char，char不能是负数  nyte【-128 =》127】  short+byte=int
b = short + int  => int  数值相加默认是int


--------------------------------------------------------------scala-------------------------------------------------------------
完全面向对象，没有基本数据类型
数值型默认都是int，加L变Long
浮点型默认Double，加f变float
强制转化 1.8.toInt  =>  1
标识符：可以是+-*/@等，首字符必须是，不能和字母数字下划线一起，单独下划线不行
关键字：val,var，object，def，implicit
运算符：无++i，i++ ；  只有+=，-+，有数据类型提升
scala任意表达式都有返回值：if else默认最后一行 三元表达式：var aa = if（flag）1 else 0
unit =》print()  


--------------------------------------------------------------函数function-------------------------------------------------------------
无重载：函数重名报错  
惰性：java无 ，spring有  lazy val 
访问权限： public,ptotect(只能子类访问),private(只能当前类访问),缺省private【同包，指定包】
方法（method）：类中的函数（function），需要new
构造方法：构造器（参数个数）scala：主，辅构造器
类：动态绑定，方法&属性重写
调用顺序：父类构造方法 =》子类构造方法 =》辅助构造方法
高阶函数：函数的返回结果是另一个函数本身

--------------------------------------------------------------特质trait-------------------------------------------------------------
java接口：1.不能直接new  2.只能抽象方法  3.可以声明属性值，但无法修改
scala特质：1.            2.属性和方法直接调用（无重写，往上寻找）
特质特点：1.是可以执行的代码（可以执行代码）
执行顺序：继承主类 > trait1 > trait2 > 当前。  其实和往下覆盖回归
相同特质执行一遍，和类的初始化一致
特质可以继承类 


--------------------------------------------------------------隐式转换-------------------------------------------------------------
意义：功能拓展
出入口：类型，一种类型只能定义一个
隐式值：隐式值替换隐式参数   调方法不加（）  参数名前加implicit  顺序:隐式值 =》默认值
隐式类：2.10之后 。1.所带构造器参数有且只有一个 2.隐式类必须定义在类或伴生对或包里面 =》隐式类不能是顶级的  2.不能使case class（样例类）


--------------------------------------------------------------java-------------------------------------------------------------
集合：
java：
数组：Array，不可变（地址），内容可变 
集合: 可变

scala：
数组集合机会都分为可变与不可变
数组：Array ArrayBuffer
集合： seq： List，listBuffer，queue
	   set：


--------------------------------------------------------------模式匹配-------------------------------------------------------------

java:switch  deafult 执行完逻辑都加break；
match: _ => 默认 ，不用break 自动退出  
case class => 为模式匹配优化的类
偏函数：map是对所有元素执行操作，在符合某个条件，而不是所有元素执行操作时，用偏函数
闭包：一个函数与其相关的引用环境（变量/值）组合的整体
柯里化：def f1（a: int,b: Int） => def f2 (a: int)(b: int)
抽象控制：



-------------------------------------------------------------------------------后记---------------------------------------------------------------------------
list1.sortBy(_,false) 默认升序 int  =》 Boolean  适用一元数组
list1.sortwith(_.1>_.2) 自定义




















